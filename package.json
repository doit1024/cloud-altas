{
  "name": "cloud-altas",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "python3 -m http.server 4173 --directory dist"
  },
  "devDependencies": {
    "wrangler": "^4.40.2"
  }
}
